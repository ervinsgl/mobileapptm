_schema-version: "3.2"
ID: mobileapptm
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: mobileapptm-destination-content
  type: com.sap.application.content
  requires:
  - name: mobileapptm-destination-service
    parameters:
      content-target: true
  - name: mobileapptm-repo-host
    parameters:
      service-key:
        name: mobileapptm-repo-host-key
  - name: mobileapptm-uaa
    parameters:
      service-key:
        name: mobileapptm-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: mobileapptm_html_repo_host
          ServiceInstanceName: mobileapptm-html5-service
          ServiceKeyName: mobileapptm-repo-host-key
          sap.cloud.service: mobileapptm
        - Authentication: OAuth2UserTokenExchange
          Name: mobileapptm_uaa
          ServiceInstanceName: mobileapptm-xsuaa-service
          ServiceKeyName: mobileapptm-uaa-key
          sap.cloud.service: mobileapptm
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: mobileapptm-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: mobileapptm-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - mobileapptm.zip
      name: mobileapptm
      target-path: resources/
- name: mobileapptm
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: mobileapptm-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: mobileapptm-destination-service
    service-plan: lite
- name: mobileapptm-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: mobileapptm-xsuaa-service
    service-plan: application
- name: mobileapptm-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: mobileapptm-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
